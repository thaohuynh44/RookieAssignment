@model IEnumerable<MenuItemDTO>
@using RookieAssignment.Shared.DTOs

@if(Model.Count() > 0)
{
    <div class="col-12">
        <div class="row">
            <h3 class="text-success">@Model.FirstOrDefault().Category.Name</h3>
        </div>
        
        @*@foreach(var item in Model)
        {
            <div class="border border-info rounded col-12" style="margin-top:10px; margin-bottom:10px; padding:10px;">
                <div class="row">
                    <div class="col-md-3 col-sm-12">
                        <img src="@item.Image" style="border-radius:5px; border: 1px solid #bbb9b9; width:250px;" />
                    </div>
                    <div class="col-md-9 col-sm-12">

                    </div>
                </div>
            </div>
        }*@
    <div class="row">
        @foreach(var item in Model)
        {
        <div class="card">
            <img class="card-img-top" src="@item.Image" style="border-radius:5px; border: 1px solid #bbb9b9; width:250px;" />
            <div class="card-body">
                <h4 class="card-title">@item.Name</h4>
                <p class="card-text">Dddddddddddddddddddd</p>
            </div>
        </div>
        }
    </div>
    </div>

}

